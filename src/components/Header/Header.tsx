import * as React from "react";

import './Header.scss';

const Header = () => (
    <div id="tableContainer">
    <table>
        <thead>
            <tr>
                User Hobbies
            </tr>
        </thead>
    </table>
</div>
);

export default Header;
